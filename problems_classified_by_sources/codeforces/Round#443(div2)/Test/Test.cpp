#include <bits/stdc++.h>
using namespace std;
const int maxn = 100005;

int ar[maxn];

int main()
{
    int n, k, m;
    scanf("%d%d%d", &n, &k, &m);
    for(int i = 1; i <= n; i++){
        scanf("%d", ar + i);
    }

    return 0;
}
